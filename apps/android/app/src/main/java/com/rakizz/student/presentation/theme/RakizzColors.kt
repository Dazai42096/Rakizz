package com.rakizz.student.presentation.theme

import androidx.compose.ui.graphics.Color

// shared colors UI
object RakizzColors {

    // main backgrounds
    // warm ivory is easier on the eyes than pure white
    val Background = Color(0xFFF6F1E8)
    val BackgroundSoft = Color(0xFFEFE7DA)

    // cards
    // most content should sit on white cards
    val Card = Color(0xFFFFFFFF)
    val CardSoft = Color(0xFFFAF6EF)
    val CardBorder = Color(0xFFE3D8C9)

    // main brand colors
    // deep teal gives trust + focus without looking harsh
    val Primary = Color(0xFF2E5E5A)
    val PrimaryDark = Color(0xFF1F403D)
    val PrimarySoft = Color(0xFFDDEBE8)

    // warm academic accent
    // use this for badges and small highlights, not everywhere
    val Accent = Color(0xFFC9A27E)
    val AccentSoft = Color(0xFFF0DFCE)

    // text colors
    val TextMain = Color(0xFF1F2A2E)
    val TextSecond = Color(0xFF66737A)
    val TextMuted = Color(0xFF9AA3A8)

    // status colors
    // muted colors so the UI does not look stressful
    val Success = Color(0xFF4F7D5A)
    val Warning = Color(0xFFB9853D)
    val Error = Color(0xFFA94B4B)

    val White = Color(0xFFFFFFFF)
}