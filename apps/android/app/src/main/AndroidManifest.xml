<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet is needed because the app talks to the FastAPI backend -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Used to check if the phone has a network connection -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Used for assignment reminders and notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Allows reminder receivers to restart after phone reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Used for usage tracking and focus monitoring -->
    <uses-permission
        android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Allows the app to read installed apps for the parent focus feature -->
    <uses-permission
        android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Main application configuration -->
    <!-- android:icon and android:roundIcon use the custom Rakizz launcher icon -->
    <application
        android:name=".RakizzApp"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RakizzStudent"
        android:usesCleartextTraffic="true">

        <!-- FileProvider is used to safely open local material files -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">

            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Receives assignment reminder alarms -->
        <receiver
            android:name=".notifications.assignment.AssignmentReminderReceiver"
            android:exported="false" />

        <!-- Restores assignment reminders after the phone restarts -->
        <receiver
            android:name=".notifications.assignment.AssignmentReminderBootReceiver"
            android:enabled="true"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Accessibility service used for app blocking and focus mode -->
        <service
            android:name=".blocking.RakizzBlockerAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">

            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/rakizz_blocker_accessibility" />
        </service>

        <!-- Main app screen and launcher entry point -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.RakizzStudent">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>